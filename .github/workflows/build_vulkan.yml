    - name: Build with Ant directly
      run: |
        # 只编译 Java 部分（不包括原生库）
        ant clean
        ant compile
        ant jar -Dbuild.type=release
        
        # 或者尝试生成特定模块
        ant -p | grep -i shaderc
